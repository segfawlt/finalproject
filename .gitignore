# .opencode/ contains a local OpenCode install (its own package.json, node_modules, etc.)
# Track the project-specific skills and prompts; the inner .opencode/.gitignore
# handles the vendored dependencies.
.opencode/node_modules/
.opencode/package.json
.opencode/package-lock.json
.opencode/bun.lock
node_modules/
dist/
.turbo/
.env
.env.local
.env.*.local
*.log
.DS_Store
session-*.md
.grepai/
.worktrees/

# Playwright E2E (live-env report evidence — never commit captured sessions)
/e2e/.auth/
/e2e/report/
/test-results/
/playwright-report/

về khái niệm actors, nếu được hỏi đến thì với hệ thống này chỉ có admin là actor, còn AI provider và Discord API là external services
khái niệm Container khi em viết caption cho hình thể hiện kiến trúc là gì? cô không hiểu khái niệm Container em dùng ở đó
trạng thái mong muốn của hệ thống được lưu lại ở đâu để khi roll back có thể lấy ra, trong đoạn này cô chưa thấy làm rõ: 
5.5.1 Preview and manual iteration
The latest persisted DesiredState is rendered in DesiredStateView. Existing current
state is passed alongside it so the desired-state primitives can mark additions, edits,
and deletions. The same representation includes tombstones, member-role
assignments, and permission-related channel state. The administrator may revise
through natural language, revert to a prior iteration, or enter manual edit mode. Manual
edits are posted back to the conversation and stored as a new manual_edit iteration;
they do not edit Discord directly.
không thấy có Sitemap và wireframe minh họa UI design ở mục Thiết kế nhỉ
Screenshots em đặt luôn trong phần mô tả từng chức năng chứ, em để file ảnh ở đâu vậy? cần đặt trong phần mô tả chức năng đã cài đặt nhé
Phần testing em có mục Automated test cases, có đúng là các test cases đó được thực hiện tự động không? hay em thực hiện?
tự động có nghĩa là em có kịch bản test và em dùng tool để thực hiện test theo kịch bản có sẵn
còn nếu em tự tương tác trên giao diện để thực hiện chức năng và quan sát kết quả thì là manual test
em trình bày lại cho chính xác nhé
về bảng mô tả các test cases, mục input nếu có thể mô tả dữ liệu vào cụ thể hơn ví dụ em yêu cầu tạo channel thì đầu vào là "create IT Devs channel"
thiếu Conclusion, References, cover page, abstract (check report_structure again)

